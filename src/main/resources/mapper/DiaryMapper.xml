<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.domain.diary.DiaryMapper">

    <select id="findByDate" parameterType="java.time.LocalDate" resultType="com.example.demo.domain.diary.Diary">
        SELECT id, diary_date AS diaryDate, title, content
        FROM diary
        WHERE diary_date = #{date}
    </select>

    <insert id="insert">
        INSERT INTO diary (diary_date, title, content)
        VALUES (#{date}, #{title}, #{content})
    </insert>

    <update id="update">
        UPDATE diary
        SET title = #{title}, content = #{content}
        WHERE diary_date = #{date}
    </update>

    <select id="findByMonth" resultType="com.example.demo.domain.diary.Diary">
        SELECT id, diary_date AS diaryDate, title, content
        FROM diary
        WHERE diary_date BETWEEN #{start} AND #{end}
        ORDER BY diary_date ASC
    </select>

</mapper>